target_id: lab_agent
corpus_dir: ../promptfuzzr/corpus/seeds
axes: [delivery, encoding]

retry_policy:
  max_retries: 8
  backoff_seconds: 1.5

authority_policy:
  role: support_agent
  allowed_tools:
    - lookup_order
    - get_weather

concurrency: 4
